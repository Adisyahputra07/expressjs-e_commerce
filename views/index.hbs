{{>header}}
{{>navbar}}
<div class="container mt-5 mb-5">
  <section> 
  <div class="container">
    <div class="row mt-1">
      <div class="col-12 align-items-center" >
      </div>
    </div>
  </div>
</div>
  </section>
  <section>
        <div class="container">
          <div class="row mt-1">
            <div class="col">
              <h3>Let's Order</h3>
            </div>
          </div>
          {{#if datas}}
          <div class="row">
            {{#each datas}}
            <div class="col-3 mt-2 mb-5">
              <div class="card">
                <img src="../uploads/{{this.photo}}" class="card-img-top" alt="bmw" />
                <div class="card-body">
                  <h5 class="card-title">{{this.name}}</h5>
                  <p class="card-text">Brand: {{this.brand_name}}</p>
                  <p class="card-text">Price: {{this.price}}</p>
                  <a class="nav-link btn btn-dark" style="color: whitesmoke;" href="/login">Buy Now</a>
                </div>
              </div>
            </div>
            {{/each}}
          </div>
          {{/if}}
        </div>
  </section>
</div>
